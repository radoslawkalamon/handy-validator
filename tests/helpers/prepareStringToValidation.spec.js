/* eslint-disable no-undef */
/* eslint-disable no-console */

import prepareString from '../../src/helpers/prepareStringToValidation';

describe('HELPERS: prepareStringToValidation', () => {
  it('ToDo', () => {
    expect(true).toBe(true);
  });
});
